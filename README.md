# Amethyst

I fixed the AUR Helper Amethyst from Crystal Linux myself because its likely that it won't get updated.

compile Amethyst by doing

``cargo build --release``

then do 

``sudo mv ~/amethyst/target/release/ame /usr/bin/``
